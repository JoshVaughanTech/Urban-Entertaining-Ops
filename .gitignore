node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel
coverage/

.pglite/
devdb.log

# Every env file stays local. The example is committed on purpose — it is a
# template with no real values in it — so it is re-allowed below.
.env
.env.*
!.env.local.example
